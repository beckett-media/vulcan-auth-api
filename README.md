# API server for blockchain NFTs

## Setup PoC demo API server

The poc demo API server uses json-server. It can automaticlly convert json file into restful API endpoints.

1. npm install

2. npx json-server mockup/apes.json --routes mockup/routes.json
